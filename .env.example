# Copy to .env and edit. An EMPTY value always means "use the built-in
# default", never "empty string as the value".

# The address game clients use to reach this host (LAN/VPN/public IP).
# Default when empty: 127.0.0.1 (same-machine testing only).
FE_ADVERTISE=
